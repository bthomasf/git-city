## 1. GitLab 接入与数据同步

- [x] 1.1 在 `.env.local` 或相关配置文件中新增 `GITLAB_BASE_URL`、`GITLAB_TOKEN` 等环境变量，并为内网 GitLab 填入示例配置
- [x] 1.2 在后端创建 GitLab API 客户端模块（如 `gitlabClient`），封装基础请求、鉴权与错误处理逻辑
- [x] 1.3 实现多 Group / 多项目配置解析逻辑，支持从配置中读取需要纳入统计的 Group 与项目列表
- [x] 1.4 实现 GitLab 活动数据增量同步 Job（定时任务或后台任务），拉取 commit、Merge Request、Review、Issue 等事件
- [x] 1.5 为同步任务增加失败重试与错误日志记录，并将关键错误对接到现有监控/告警体系

## 2. 贡献度模型与聚合层

- [x] 2.1 定义统一的内部事件模型（CommitEvent、MergeRequestEvent、ReviewEvent、IssueEvent）
- [x] 2.2 基于 specs 中的加权规则实现贡献度与活跃度计算函数
- [x] 2.3 引入项目权重与行为权重配置，并实现配置驱动的加权逻辑
- [x] 2.4 实现成员维度与项目维度的聚合查询接口，为前端城市视图提供统一数据结构

## 3. 城市视图与前端交互

- [x] 3.1 设计并实现新的「团队 GitLab 城市」入口路由与页面骨架
- [x] 3.2 扩展现有城市渲染组件，使其支持团队城市布局（按 Group/项目街区划分）与成员楼宇簇展示
- [x] 3.3 在团队视图中实现时间范围选择器（近 7 天 / 30 天 / 自定义区间）及数据刷新逻辑
- [x] 3.4 实现成员楼宇点击/搜索跳转个人城市视图的交互
- [x] 3.5 在个人城市视图中实现成员画像面板（近期活跃度趋势、参与项目 TopN、协作伙伴 TopN）

## 4. 协作网络与风险提示可视化

- [x] 4.1 在后端聚合成员之间的 Review/MR 协作关系，输出适合可视化的图结构数据
- [x] 4.2 在前端团队城市视图中接入协作连线可视化（线条/光束），支持开关与密度控制
- [x] 4.3 在后端实现不活跃项目与 Bus Factor 风险识别逻辑
- [x] 4.4 在城市视图中为不活跃项目与单人主导项目添加风险标识与中文说明 tooltip

## 5. 本地化与配置/权限

- [x] 5.1 引入或扩展现有 i18n 方案，新增 `zh-CN` 语言包并作为内网默认语言
- [x] 5.2 将现有城市页面与新团队城市页面中的文案抽离到多语言资源文件，移除硬编码英文字符串
- [ ] 5.3 对接现有登录/鉴权体系，建立登录用户与 GitLab 用户名的映射关系（如适用）
- [ ] 5.4 在数据查询与视图渲染时应用权限过滤，只展示当前用户有权限访问的项目与数据
- [x] 5.5 编写配置使用说明文档，明确如何在不同环境配置 GitLab 接入与刷新频率参数

## 6. 部署、测试与文档

- [ ] 6.1 更新 CI/CD 配置，确保内网部署环境正确注入 GitLab 相关环境变量
- [ ] 6.2 为核心贡献度计算、数据同步与权限过滤逻辑编写单元测试
- [ ] 6.3 为城市数据 API、个人城市视图与团队过滤功能编写集成测试或端到端测试（如可行）
- [x] 6.4 在 `docs/PROJECT-DESIGN.md` 或新增文档中补充团队 GitLab 城市的架构与使用说明（中文）
- [x] 6.5 在测试环境完成一次从零部署与数据拉取验证，并记录操作步骤与常见故障排查建议

